rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Vercel API (Firebase Admin SDK bypasses these rules).
    // Block direct client reads/writes so the database is not public.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
