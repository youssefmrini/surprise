# Copy to .env.local for local dev, then run: npx vercel dev
# (Never commit .env.local — it is gitignored.)

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret
